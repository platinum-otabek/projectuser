extends layout
block content
    div.m-5
        h1 Tizimga kirish
        form(method="POST", action="/login")
            .form-group
                label(for="") Foydalanuvchi ismi
                input.form-control(type="text", name="username")
            .form-group
                label(for="") Password
                input.form-control(type="password", name="password")

            input.btn.btn-success(type="submit", value="Ro'yxatdan o'tish")

