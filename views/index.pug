extends layout
block content
    h1.m-4 #{title}
    ul.list-group.m-4
        each user,i in users
            li.list-group-item #{user.title} 
                a.mx-5(href="/users/delete/" + user._id,  ) deleted
                a.mx-5(href="/users/update/" + user._id,  ) update 